import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        padding: 10,
        alignItems: 'center',
        marginTop: 20
    },
    buttonText: {
        fontSize: 16,
        fontWeight: 'bold'
    },
})