import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        padding: 15,
        borderRadius: 7,
        alignItems: 'center',
        marginTop: 20
    },
    buttonText: {
        fontSize: 17,
        fontWeight: 'bold'
    },
})